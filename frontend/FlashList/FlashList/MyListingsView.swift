import SwiftUI

struct MyListingsView: View {
    var body: some View {
        VStack {
            Text("My Listings")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
    }
}

#Preview {
    MyListingsView()
} 